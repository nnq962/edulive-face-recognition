import React from 'react';

const Settings: React.FC = () => {
    return (
        <div>
            <h2>Cấu hình</h2>
            <p>Các thiết lập hệ thống (mock): ngôn ngữ, giờ làm việc, v.v.</p>
        </div>
    );
};

export default Settings;

